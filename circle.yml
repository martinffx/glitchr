machine:
  services:
    - docker
  environment:
    PATH: "$HOME/.asdf/bin:$HOME/.asdf/shims:$PATH"

dependencies:
  cache_directories:
    - ~/.asdf
    - ~/docker
    - deps
    - _build
  pre:
    - chmod +x ./circle_pre_build.sh
    - ./circle_pre_build.sh
  post:
    - chmod +x ./circle_post_build.sh
    - ./circle_post_build.sh

database:
  override:
    - psql -U ubuntu < ./db/init-test.sql

test:
  override:
    - mix test
  post:
    - mix docker up; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:4000

deployment:
  prod:
    branch: master
    commands:
      - mix release
      - mix deploy
